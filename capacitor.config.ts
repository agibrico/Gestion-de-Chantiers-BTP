import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'ci.agb.chantier',
  appName: 'AGB Chantier',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};
export default config;
